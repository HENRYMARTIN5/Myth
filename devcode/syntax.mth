~ This is what the future syntax should look like.

var a = [1]
var b = [1]

if ({a} == {b}) do
    out A is equal to b!

var c = y

out Please enter "y" or "n".

var userinput = {GetUserInput}

if ({userinput} == {c}) do
    out You entered the string:
    outvar userinput
elseif ({userinput} == {d}) do
    out You entered the string:
    outvar userinput
else do
    out You did not enter the string "y" or "n".

var d = n
